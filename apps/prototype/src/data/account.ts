import type { Platform } from './types';

export const account = {
  connectedPlatforms: ['google_ads', 'meta_ads'] as Platform[],
  timeZone: 'Asia/Dhaka',
};

export const platforms: Platform[] = ['google_ads', 'meta_ads', 'microsoft_ads'];
